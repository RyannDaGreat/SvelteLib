# Lazily-built proxies + frame JPEGs — regenerated on demand, never committed.
cache/*
!cache/.gitkeep
# Annotation data is user output, not source. Keep the folder, ignore the JSONs.
outputs/*.json
# Build / deps
web/dist/
node_modules/
__pycache__/
.venv/
