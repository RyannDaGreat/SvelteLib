{
  "name": "powerrp-desktop",
  "productName": "PowerRP",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop shell for PowerRP: an Electron window over the existing web app, with the whole server stack as a child process that quits with the app.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "node write_repo_path.js && electron-builder --dir",
    "package:dmg": "node write_repo_path.js && electron-builder"
  },
  "devDependencies": {
    "electron": "^31.3.0",
    "electron-builder": "^24.13.3"
  }
}
