<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" width="100" height="100">
  <!-- The lightning bolt from the retired legacy shape table, kept VERBATIM: the
       path data is core/shapes.js lightningPathD(100, 100) byte for byte. It
       lives here as clip-art rather than as a shapeshifter family because there
       is no parameter worth exposing on it (user: "maybe better as a preset for
       an SVG") — a zigzag's vertices are the drawing, not a family of drawings. -->
  <path d="M60 0 L20 55 L45 55 L35 100 L80 40 L50 40 L70 0 Z" fill="currentColor"/>
</svg>
