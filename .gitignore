node_modules/
dist/
# The PowerRP static bundle (vite build of src/demo_apps/PowerRP/web). Built by
# the Pages workflow on every push, so it is a build artifact, never committed —
# it is ~90 MB (canvaskit.wasm plus 50 bundled font files).
dist-powerrp/
package-lock.json
.claude_logs/
.claude_vlm_checks/
.claude_todo.md
.claude_todo*.md
.claude_diffs/
scratchpad*.py
scratchpad*.js


#<RP Default Gitignore Start>
*.pyc
*.bak
*.swh
*.swi
*.swj
*.swk
*.swl
*.swm
*.swn
*.swo
*.swp
*.un~
*.gstmp
*.ipynb_checkpoints
*.DS_Store
~$*.pptx
*.blend1
.frenzy
#<RP Default Gitignore End>


# Test video assets (large, not checked in)
public/videos/
.claude/

# editor junk
*~
.*.un~
*.swp

# Stray probe-artifact dirs (must go to .claude_logs or scratch, never repo tree)
.claude_shots/
.probe_*/

# Agent scratch dirs (evidence/probes) — never the repo tree
.scratch/
scratchpad*.mjs

# scratch worktree for baseline comparison (never committed)
.baseline_wt
