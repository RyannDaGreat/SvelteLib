# SpeechToText component (separate task — do not touch video_slice_annotator)

Goal: client-side live speech-to-text via Web Speech API (Chrome/Edge native,
interim word-by-word streaming). Headless component + demo with spacebar
record/stop appending to a log of texts. Engine chosen by user: Web Speech API.

Files I own (new, isolated):
- src/lib/SpeechToText.svelte         — headless component wrapping SpeechRecognition
- src/demos/SpeechToText/demo.html
- src/demos/SpeechToText/demo.js
- src/demos/SpeechToText/Demo.svelte  — spacebar toggle, live interim, log list
- src/App.svelte                      — append ONE hub entry line (shared file; minimal edit)

## Tasks — ALL DONE
- [x] Write SpeechToText.svelte (headless, snippet children(state, actions))
- [x] Write demo (html/js/svelte) — spacebar record/stop, log of texts
- [x] Register in hub (src/App.svelte)
- [x] Verify: vite build clean ✓; real Chrome + fake-audio puppeteer run ✓
      (mounts, supported=true, mic-click → final transcript appended to log,
      stop toggles off); spacebar toggle false→true→false ✓
