# Dropdown Generic Extension — TODO (DONE)

- [x] 1. Extend src/lib/Dropdown.svelte
  - [x] Multi-select: `multiple` prop, array value, toggle w/o close, `summary` prop, mdi:check indicator
  - [x] Inserts: `{insert: Snippet|string}` entries render between rows, skipped by nav/selection
  - [x] scrollToValue prop: scroll target into view centered on open
  - [x] Pure helpers with doctests (verified); loud error on multiple+non-array; new CSS custom props; doc comment updated
- [x] 2. Extend src/demos/Dropdown/Demo.svelte with 4 new-capability sections + dotted-insert adapter CSS
- [x] 3. Puppeteer tests src/demos/Dropdown/test_dropdown.js — 24 passed, 0 failed
  - [x] (a) single-select unchanged; (b) multi toggle+summary; (c) inserts skipped; (d) scrollToValue
- [x] 4. `npx vite build` — zero NEW warnings from my files (2 Dropdown a11y warnings are pre-existing, identical markup from git HEAD)
- [x] 5. Report delivered
